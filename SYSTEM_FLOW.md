# 系统全链路说明

这份文档是给第一次接触这个系统的人看的。

你不需要先懂代码，也不需要先懂“规则引擎”“语义复判”“账号复盘”这些词。

先记住一句话：

**这个系统不是帮你“钻审核空子”，而是帮你更稳地做内容合规，并把真实反馈、有效样本和下一篇线索持续沉淀下来。**

---

## 1. 这个系统现在主要在做什么

可以把它理解成一个围绕小红书敏感内容创作的本地工作台。

它现在有 7 条核心能力：

1. `内容检测`
   判断当前标题、正文、封面、标签有没有明显风险。
2. `合规改写`
   如果有风险，系统会按轮次和问题点继续修稿。
3. `生成新内容`
   从零起稿，或基于草稿 / 灵感 / 复盘卡继续写。
4. `反馈回流`
   把真实违规、真实放行和误报样本沉淀下来。
5. `样本学习`
   把参考样本、生命周期、风格画像、主题灵感、账号级复盘统一收束。
6. `账号诊断`
   查询账号状态、同阶对标和同类爆文信号，补充下一篇方向判断。
7. `同类爆文发现`
   在内容工作台下方独立浏览同赛道爆文，支持手动刷新和手动筛选。

---

## 2. 你可以把它理解成 7 条主链路

### 2.1 内容检测链路

你输入一篇笔记，系统告诉你：

- 有没有风险
- 风险大概在哪
- 为什么这么判断
- 可以往什么方向改

当前这条链路已经不只是一层本地规则。

它现在有两层证据：

1. `本地规则检测`
   使用系统自己的词库、白名单、上下文规则和误报/参考样本信号。
2. `外部违禁词检测`
   额外调用小红书外部违禁词库，补充词级别命中、替换建议和优化文案。

这两层会一起影响最终结论。

### 2.2 自动改写链路

如果你点“一键合规改写”，系统会：

- 先给你一版更稳妥的内容
- 如果第一版还不够稳，就根据上一轮问题继续修
- 尽量保留原来的风格、语气和信息量
- 同时按小红书成稿格式补齐有钩子的标题、至少 5 个不重复 emoji 和 3-6 个 tags

### 2.3 生成新内容链路

如果你要直接起稿，系统会：

- 结合当前输入的 brief、合集类型、标签和素材
- 如果能识别出选题关键词，会拉取近期小红书爆文数据并提炼 `爆款公式来源`
- 参考高权重样本、风格画像、主题灵感、草稿区素材
- 产出多个候选稿，再给出推荐版本

### 2.4 违规回流链路

如果平台真的处罚了内容，系统会：

- 记录处罚样本
- 分析系统哪里没看出来
- 生成候选词和候选语境
- 交给人工复核

### 2.5 误报回流链路

如果系统觉得风险高，但平台其实放行了，系统会：

- 把“可能偏严”的样本记下来
- 用于后续人工复盘
- 作为白名单候选和边界提醒证据

### 2.6 样本复盘 / 账号诊断链路

这条链路负责回答两个问题：

- 这个账号现在什么内容更稳？
- 下一篇更值得往哪个方向做？

它会结合：

- 参考样本
- 效果好样本
- 已发布但还在观察的样本
- 外部参考样本
- Redfox 账号数据
- 同阶 / 高阶对标
- 同类起量信号

### 2.7 同类爆文发现链路

这条链路是放在内容工作台下面的独立发现区。

它负责做的不是账号整体诊断，而是：

- 直接浏览同赛道爆文样本
- 手动切换今日起量 / 7 日稳定 / 低粉高表现
- 需要时手动补充赛道、关键词和标签
- 在不进入账号诊断结果面板的前提下，单独刷新一批可参考的外部信号

它和账号诊断会复用同类信号能力，但前端入口、接口和缓存都是独立的。

---

## 3. 页面上你最常用的 6 个动作

### 3.1 运行检测

你在页面里填：

- 标题
- 正文
- 封面文案
- 合集类型
- 标签

然后点“运行检测”。

系统会返回：

- `综合结论`
- `规则命中`
- `规则建议`
- `语义判断`

其中 `规则检测` 这一块，现在还会额外展示：

- 外部违禁词摘要
- 命中词与替换建议
- 外部检测是否抬高了结论
- 外部检测失败时的 `结果不完整` 提示

你可以把它理解成：

**先给你一个内容体检报告。**

---

### 3.2 一键合规改写

如果你觉得原内容确实有风险，可以点“一键合规改写”。

它现在不是简单“整篇重写 3 次”，而是更接近一个逐轮修稿编辑：

1. 先给你一版完整改写
2. 如果还没过，就总结上一轮没过的具体原因
3. 下一轮优先只修真正有风险的地方
4. 直到通过、接近通过，或者达到轮次上限

重点不是“改几次”，而是：

**每一轮都根据上一轮的问题继续定向修。**

---

### 3.3 生成新内容

如果你切到“生成新内容”，系统支持两种主要模式：

- 从零生成
- 基于现有草稿优化

生成时会综合参考：

- 当前 brief
- 合集类型与标签
- 样本库里的高权重参考内容
- 当前风格画像
- 近期爆文的标题、开头、结构、标签和互动话术规律
- 主题灵感
- 草稿区中已收集的线索

生成结果不是单稿，而是：

- 多个候选版本
- 每版带风险判断和风格 / 完整度评分
- 系统推荐其中更稳的一版

---

### 3.4 运行账号级复盘

你可以在样本区运行账号级复盘。

这条链路的目标不是“总结一句废话”，而是给你产出：

- 当前账号最近更稳定的方向
- 近期明显缺的角度
- 3-5 张可以直接回填生成工作台的下一篇建议卡

重要变化：

- 账号级复盘优先看 `参考样本` 和 `效果好样本`
- 如果前端只上传了摘要，不会再把整篇正文重复传给复盘接口
- **如果模型报错、没配置、或没返回可用复盘卡，现在会直接报错，不再自动兜底成固定模板卡**

也就是说：

**账号级复盘现在宁可明确失败，也不再假装给你一张“看起来像结果”的模板卡。**

---

### 3.5 运行账号诊断

账号诊断是另一条独立链路，和账号级复盘互补。

它负责看：

- 账号最近 30 天的基础数据
- 粉丝、互动、收藏、发布频率
- 同阶账号
- 高阶标杆账号
- 同类今日起量 / 七日稳定 / 低粉可复制信号

它支持：

- 单账号诊断
- 多账号对比
- 最近一次结果缓存
- HTML / JSON 报告落盘
- 30 分钟后自动补采重查

---

### 3.6 浏览同类爆文专区

如果你已经在内容工作台里写稿、改稿，想直接找同类爆文参考，不需要先跑账号诊断。

你可以直接在主工作台下方打开 `同类爆文专区`：

- 输入小红书号，让系统优先自动匹配账号赛道
- 或者手动补充赛道、关键词、标签
- 点一次 `手动刷新`
- 再按 `今日起量 / 7日爆文 / 低粉高表现` 切换浏览

这一块的定位更像：

**写稿过程中的独立发现区，而不是账号诊断结果里的附属模块。**

---

## 4. 自动改写现在到底怎么工作

这是最容易被误解的一块。

很多人会以为：

“系统是不是就傻傻地连续改 3 次？”

不是。

现在真实逻辑更接近下面这样。

### 4.1 第 1 轮

先给你一版完整改写。

目的很简单：

- 先把明显风险拉下来
- 先生成一个更稳妥、同时可直接作为小红书成稿基础的版本
- 标题必须有钩子，正文与标题要自然带 emoji，tags 要补齐到 3-6 个

### 4.2 第 2 轮开始

如果第 1 轮还没过，系统不会默认再整篇重写一次。

它会优先做局部修补：

- 哪一句有问题，就改哪一句
- 哪个标题太擦边，就只收紧标题
- 哪段正文有导流感，就只修那一段

这就是现在的 `patch` 思路。

### 4.3 为什么这样做更好

因为这样有 3 个好处：

1. 更省 token
2. 更不容易改跑风格
3. 更容易解释每一轮在修什么

---

## 5. 为什么不是每一轮都跑交叉复判

因为交叉复判更重，也更贵。

当前策略是：

### 5.1 明显还没过

如果本地综合判断还是：

- `manual_review`
- `hard_block`

那就先继续改，不急着跑交叉复判。

### 5.2 已经接近通过

如果本地综合判断已经到了：

- `observe`
- `pass`

这时候再跑交叉复判，做最后一道更保守的确认。

### 5.3 到最后一轮

如果已经到最大轮次了，不管过没过，系统都会补跑一次交叉复判，再给出最终结果。

所以当前逻辑是：

**先用轻一点的方式把稿子修到差不多，再用重一点的方式做最后把关。**

---

## 6. 前端现在为什么比以前更不黑箱

改写结果区现在不仅给你最终文案，还会给你每一轮的复盘信息。

每轮会看到 3 类内容：

### 6.1 系统建议

这一轮系统最想让模型优先修什么。

比如：

- 去掉导流感
- 弱化功效承诺
- 收紧标题的擦边氛围

### 6.2 实际修改

模型这一轮到底改了什么。

尽量让你能看见：

- 改了哪一段
- 改了哪一句
- 这次改动是在回应哪条建议

### 6.3 剩余风险

为什么改完这一轮，系统还是没放行。

比如：

- 标题还是有轻微挑逗感
- 正文里还有弱导流倾向
- 整体语气还是偏擦边

这套展示的目标很简单：

**让你知道系统不是在瞎改，而是在按问题逐轮修。**

---

## 7. 违规回流链路是什么

这条链路处理的是：

**平台真的处罚了你的内容。**

### 7.1 你会输入什么

通常会输入：

- 笔记内容
- 平台违规原因
- 可选截图

### 7.2 系统会做什么

系统会做 4 步：

1. 先把样本记下来
2. 再看现有规则有没有命中
3. 结合平台原因补值得关注的候选词和候选语境
4. 放进人工复核队列

### 7.3 为什么不直接自动入库

因为平台原因并不总能直接变成规则。

常见风险有：

- 平台给的是很抽象的标签
- 模型可能会补出噪音短语
- 某条规则可能写得太宽

所以这条链路的原则一直是：

**模型负责提建议，人负责最后决定。**

---

## 8. 误报回流链路是什么

这条链路处理的是：

**系统觉得有风险，但平台其实放行了。**

### 8.1 什么时候适合记录误报样本

比如：

- 系统给了 `manual_review`
- 甚至给了 `hard_block`
- 但你发出去之后，平台没有处罚

### 8.2 记录之后能干嘛

系统会：

1. 保存内容
2. 保存检测快照
3. 标记成待观察或已确认
4. 供后面继续复盘

### 8.3 为什么不会自动放宽规则

因为一条误报样本，不足以说明规则真的错了。

它只能说明：

**这是一条值得人工再看一眼的证据。**

---

## 9. 什么叫人工复核

这个系统里，“人工复核”不是多余步骤，而是非常关键的一层。

前面的规则和模型负责：

- 发现问题
- 给建议
- 提供证据

最后真正影响长期规则质量的，是人。

### 9.1 人工复核主要看什么

主要看两类东西：

1. `违规回流出来的候选词 / 候选语境`
2. `误报回流积累下来的偏严证据`

### 9.2 人工复核最后决定什么

人工会决定：

- 哪些候选词值得入库
- 哪些建议只是噪音
- 哪些规则需要保留
- 哪些规则可能需要放松一点

所以真正稳定的关键不是模型本身多聪明，而是：

**模型帮你提建议，人帮你守住最终质量。**

---

## 10. 数据现在主要记到哪里

当前系统最重要的事实层已经统一到 `note-records`。

### 10.1 统一主存储

现在页面里的：

- 参考样本
- 生命周期记录
- 样本基础内容
- 预判复盘信息

底层都统一落到：

- `data/note-records.json`

### 10.2 兼容视图，不再是主入口

下面两份文件还会存在：

- `data/success-samples.json`
- `data/note-lifecycle.json`

但它们现在只是兼容旧结构的派生视图，不再是新的主存储入口。

### 10.3 其他关键数据文件

| 文件 | 你可以把它理解成什么 |
| --- | --- |
| `lexicon.seed.json` | 系统自带的基础规则库 |
| `lexicon.custom.json` | 你自己确认过、长期要用的规则库 |
| `whitelist.json` | 明显偏安全的表达或语境 |
| `feedback.log.json` | 平台处罚样本日志 |
| `review-queue.json` | 等待人工复核的候选项 |
| `false-positive-log.json` | 系统可能偏严的误报样本 |
| `rewrite-pairs.json` | 改写前后对照样本 |
| `draft-ideas.json` | 草稿区待写选题池 |
| `theme-inspirations.json` | 主题灵感缓存 |
| `account-planner-summary.json` | 最近一次账号级复盘结果 |
| `xhs-account-diagnosis.json` | 最近一次账号诊断结果 |
| `xhs-account-diagnosis-subscriptions.json` | 账号诊断补采任务 |
| `inner-space-terms.json` | 赛道术语、别名和提示语 |

---

## 11. 样本学习和下一篇建议现在怎么衔接

当前一条内容从进入系统到反哺下一篇，大致会走这条路径：

1. 先写内容，跑检测 / 改写 / 生成
2. 需要时保存到生命周期
3. 发布后回填真实结果
4. 如果足够稳定，可以启用参考属性
5. 进入风格画像、主题灵感、账号级复盘
6. 复盘卡 / 灵感卡 / 候选稿可以一键进草稿区
7. 草稿区再一键回填生成工作台

所以这套系统不是单点工具，而是一个闭环：

**检测 -> 改写 / 生成 -> 发布 -> 回流 -> 学习 -> 下一篇**

---

## 12. 账号复盘和账号诊断有什么区别

这两个名字很像，但职责不一样。

### 12.1 账号级复盘

更偏内容策略层。

它主要看：

- 本地高权重样本
- 参考样本
- 效果好样本
- 外部参考样本

它输出的是：

- 下一篇建议卡
- 标题公式
- 正文结构
- 边界提醒
- 可直接回填生成工作台的预填信息

### 12.2 账号诊断

更偏账号数据层。

它主要看：

- 账号基础信息
- 近 30 天指标
- 同阶对标
- 高阶标杆
- 同类起量信号

它输出的是：

- 优势
- 风险
- 下一步动作
- 对标与信号面板
- HTML / JSON 报告

简单理解：

- `账号级复盘` 更像“下一篇怎么写”
- `账号诊断` 更像“这个号现在处在哪个阶段”

---

## 13. 测试和真实数据现在怎么隔离

这是最近特别重要的一点。

测试环境里会把这些文件路径切到临时目录：

- `note-records.json`
- `theme-inspirations.json`
- `xhs-account-diagnosis*.json`
- 其他相关 `data/*.json`

目的很简单：

**测试示例、占位标题和假数据，不能再写回真实业务文件。**

所以现在真实数据应该只来自：

- 页面上的正式操作
- 正式导入流程
- 真实 API 调用

而不是测试辅助函数。

---

## 14. 如果从产品视角看，这个系统的核心价值是什么

可以总结成 4 句话。

### 14.1 它先帮你发现问题

不是等平台处罚以后才知道出事，而是先在本地做一轮预判。

### 14.2 它再帮你更稳地修改或重写

不是无脑重写，而是尽量保留原来的内容风格，逐轮定向修。

### 14.3 它会把真实反馈和样本沉淀下来

不管是处罚样本、误报样本、参考样本还是生命周期结果，最后都不会白白浪费。

### 14.4 它还能把沉淀下来的经验反哺下一篇

通过风格画像、主题灵感、账号级复盘、账号诊断和草稿区，把经验继续喂回生成链路。

---

## 15. 最后记住这 8 句话就够了

1. `运行检测` 是先看风险，不是最后判决。
2. `自动改写` 现在是逐轮定向修，不是盲目重写。
3. `patch` 的意思就是“只改真正有问题的地方”。
4. `交叉复判` 是最后把关，不是每轮都要跑。
5. `违规回流` 和 `误报回流` 都是在帮系统积累真实样本。
6. `note-records.json` 现在是样本事实层的统一主存储。
7. `账号级复盘` 出错时现在会直接报错，不再偷偷兜底成模板卡。
8. `人工复核` 仍然是正式沉淀规则前的最后一步。
